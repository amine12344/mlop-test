apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mlop-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_USER/mlop-test.git
    targetRevision: main
    path: helm/mlop-test
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: mlop-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true